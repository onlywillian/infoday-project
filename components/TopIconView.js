import { View } from 'react-native';

import Button from './Button';

export default function SpriteView() {
    return (
        <View style={{ marginLeft: 20 }}>
            <Button iconName='backpack'/>
        </View>
    )
}